# Important: In production, authorize only a specific IP address or range of addresses to access your instance.
# For security reasons, it is not recommended to allow SSH access from all IP addresses (0.0.0.0/0) to your instance, 
#   except for testing purposes and only for a short time.
ssh_allowed_cidr="0.0.0.0/0"
http_allowed_cidr="0.0.0.0/0"